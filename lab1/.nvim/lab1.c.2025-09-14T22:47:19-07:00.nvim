#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
int main() {
  FILE *stream;
  char *line = NULL;
  size_t len = 0;
  ssize_t text;
  printf("Please enter some text: ");
  text = getline(&line, &len, stream);
  printf("%zd:\n", text);
  free(line);
  fclose(stream);
}
