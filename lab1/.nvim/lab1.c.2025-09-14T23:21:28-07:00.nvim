#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
int main() {
  char *buff = NULL;
  size_t size = 0;
  printf("Please enter some text: ");
  ssize_t text = getline(&buff, &size, stdin);
  printf("%s:\n", buff);
  printf("%zd:\n", text);
  free(buff);
}
